# 홈 화면과 소리 크기 표현 개선 구현 기록

## 구현 내용

- 헤더의 빠른 이동 메뉴에 `홈 화면` 버튼을 추가했습니다.
- 진행 중 홈 화면을 누르면 기록이 사라질 수 있다는 확인 창을 먼저 보여 줍니다.
- 미션 1의 `높아짐·낮아짐` 표현을 `작아짐·같음·커짐`으로 바꾸고, 설명에 `소리 크기 표시`를 넣었습니다.
- `음높이`와 `소리 크기`를 구분하도록 미션 1의 고정 조건과 단서를 고쳤습니다.
- 파동 막대는 크기에 따라 높이만 달라지고, 애니메이션 속도는 일정하게 유지되도록 수정했습니다.
- 업데이트 내역에 2026-08-16 변경 사항을 추가했습니다.

## 과학적 판단

주파수 변화는 음높이와 관련되고, 진폭·음압 변화는 소리 크기와 관련됩니다. 이 앱은 실제 소리·데시벨을 측정하지 않으므로 `소리가 커짐`보다 `소리 크기 표시가 커짐`을 사용했습니다.

## 검증

- `npm test`: 단위 테스트 22개, 빌드, 서버 렌더링 테스트 2개 통과
- `npm run typecheck` 통과
- `npm run lint` 통과
- `npm run validate:content` 통과
- `git diff --check` 통과
- 브라우저 미션 1 흐름에서 `작아짐·같음·커짐` 확인
- 375px 모바일에서 문서 폭 375px, 가로 넘침 없음
- low/high 파동 막대 높이는 10px/20px, 애니메이션 속도는 모두 1.45s
- 브라우저 콘솔 오류 0건
