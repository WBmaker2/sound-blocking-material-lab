# 소리 차단 재료 연구소 쉬운 말 개선 계획

## 목표

초등학생이 화면을 읽다가 “이게 무슨 뜻이지?” 하고 멈추지 않도록, 어려운 과학 용어를 짧고 익숙한 말로 바꾼다. 과학적으로 필요한 뜻과 “이 앱 속 모형만 비교한다”는 한계는 그대로 지킨다.

## 바꾸는 기준

- `소리원`은 처음 보일 때 `소리 내는 곳`으로 설명하고, 이후 짧게 쓴다.
- `수신기`는 `소리 받는 곳`으로 설명한다.
- `전달 시료`와 `줄임 시료`는 `재료 모형`과 `덧댄 모형`으로 바꾼다.
- `배치`는 `놓는 방법`, `변인`은 `바꾼 것`, `근거`는 `왜 그런지 보여 주는 단서`로 바꾼다.
- `예측`은 `먼저 생각하기`, `가상 시험`은 `컴퓨터 모형 실험`, `단계`는 `크기 표시`로 바꾼다.
- 한 문장에 한 가지 행동만 담고, 어려운 명사는 괄호 설명이나 짧은 뒤풀이 문장으로 풀어 쓴다.
- “완전히 막혔다”, “실제 안전”처럼 과장될 수 있는 표현은 계속 금지한다.

## 적용 범위

1. 시작·안전·안내 화면의 제목, 설명, 버튼
2. 미션 5개의 제목, 질문, 선택지, 고정 조건, 단서
3. 경로 그림, 결과 표, 비교 기록 표, 도움말·교사용 안내
4. 테스트와 업데이트 내역을 새 문장에 맞게 조정

## 확인 방법

- 학생 화면에서 한 번 읽고 행동을 고를 수 있는지 문장 검토
- 기존 한 가지 바꾸기 규칙과 모형 한계 문장이 유지되는지 콘텐츠 검증
- 타입 검사·린트·전체 테스트·브라우저 흐름 재확인
