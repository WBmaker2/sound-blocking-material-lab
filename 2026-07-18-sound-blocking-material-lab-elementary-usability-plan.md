# 소리 차단 재료 연구소 초등학생 사용성 개선 설계

- 작성일: 2026-07-18
- 대상 앱: `소리 차단 재료 연구소`
- 주 대상: 초등학교 3~4학년, 개인 또는 2인 모둠
- 선택한 접근: 학습 흐름 전체 개선안 A
- 문서 목적: 학생 관점 실사용 테스트에서 확인한 문장·기능·디자인·UI 문제를 구현 전에 확정한다.

## 1. 목표

학생이 어려운 과학 표현을 해석하느라 멈추지 않고, 화면에서 요구하는 행동과 그 이유를 바로 이해하도록 개선한다. 특히 모든 미션에서 `한 가지만 바꾸기`, `소리가 남아 있는 길 찾기`, `가상 시험의 한계 알기`를 서로 다른 학습 행동으로 분명하게 경험하게 한다.

완료 뒤 학생은 다음 흐름을 스스로 설명할 수 있어야 한다.

1. 기준과 비교에서 무엇이 달라졌는지 찾는다.
2. 결과를 보기 전에 받은 소리 단계가 어떻게 달라질지 예상한다.
3. 기준과 비교의 소리 길을 차례로 살핀다.
4. 결과를 뒷받침하는 두 근거를 모두 확인한다.
5. 화면 결과는 이 가상 시험 안에서만 비교한다는 점을 기억한다.

## 2. 실사용 테스트 범위와 증거

### 테스트 흐름

`연구 시작 → 안전 약속 3개 → 떨림 안내 → 미션 1~4 → 미션 5의 1·2차 비교 → 최종 비교 기록`

### 테스트 환경

- Codex 인앱 브라우저
- 데스크톱 기본 화면
- 모바일 375×812
- 총 6번의 기준·비교 활동 완주
- 브라우저 콘솔 오류·경고 0개
- 모바일 문서 가로 넘침 없음
- 모든 주요 버튼과 선택 컨트롤의 터치 영역 44px 이상

### 확인한 강점

- 점수나 정답 개수 대신 예측과 근거를 중심으로 진행한다.
- 잘못 두 요소를 바꾸면 다음 단계가 잠기고 이유를 알려 준다.
- 데스크톱과 모바일 모두 드래그 없이 완주할 수 있다.
- 실제 오디오·마이크·데시벨·실제 재료 성능을 사용하지 않는다.
- 화면 전환, 진행률, 재시작 확인과 모바일 헤더는 안정적으로 동작한다.

## 3. 발견한 문제

### FINDING-E01. 맞는 근거 두 개 중 하나만 고르게 함 — 높음

모든 미션의 근거 카드에는 서로 다른 역할을 하는 참인 문장 두 개가 있다. 예를 들어 `줄임 시료 하나만 더했어요.`와 `열린 틈 경로는 아직 남아 있어요.`는 둘 다 학생이 확인해야 한다. 그러나 현재는 라디오 버튼이라 하나를 선택하면 다른 하나가 해제된다.

이 구조는 학생에게 `맞는 근거 하나를 버려야 한다`는 잘못된 신호를 준다. 특히 한 변인 통제와 남은 경로 확인을 함께 배우려는 앱의 핵심 목표와 충돌한다.

### FINDING-E02. 학생 화면의 핵심 용어가 추상적임 — 높음

`상대 감지 단계`, `수치 측정`, `시험 신호`, `상대 결과`, `한 요소를 통제`는 초등 3~4학년이 행동과 바로 연결하기 어렵다. 같은 표현이 안전 화면, 예측, 결과, 최종 기록에서 반복되어 읽기 부담이 커진다.

### FINDING-E03. 안전 확인 문장이 한 번에 너무 많은 뜻을 담음 — 중간

`이 결과는 같은 가상 시험 조건에서만 비교하며, 완전한 무음이나 실제 안전을 뜻하지 않음을 확인했어요.`는 비교 범위, 무음, 안전의 세 내용을 한 문장에 담는다. 모든 미션에서 반복되므로 학생이 의미를 읽기보다 완료를 위해 체크할 가능성이 높다.

### FINDING-E04. 잘못된 선택에도 초록색 변경 표시를 사용함 — 중간

두 요소를 함께 바꾼 잘못된 선택에서 바뀐 행 두 개가 초록색으로 표시된다. 초록색은 일반적으로 성공·완료를 뜻하므로 빨간 오류 문구와 시각적 의미가 충돌한다.

### FINDING-E05. 모바일 결과 화면에서 다음 행동을 찾기 어려움 — 중간

375px 결과 화면은 약 2,003px 높이이며 기준 카드, 비교 카드, 변화 요약, 다음 버튼 순으로 이어진다. 가로 넘침은 없지만 첫 카드만 본 학생에게 아래에 비교 카드와 다음 버튼이 있다는 안내가 없다.

### FINDING-E06. 최종 기록 문장이 교사용 표현에 가까움 — 중간

`정답 수 대신 한 요소를 통제하고 경로를 확인한 과정을 모았어요.`는 활동의 취지는 정확하지만 학생에게는 `통제`가 어렵다. 완료 화면은 학생이 자신이 한 일을 쉬운 말로 되짚는 곳이어야 한다.

## 4. 검토한 접근과 결정

| 접근 | 내용 | 장점 | 한계 | 완성도 |
| --- | --- | --- | --- | ---: |
| A. 학습 흐름 전체 개선 | 문장, 근거 선택 방식, 오류 색상, 모바일 결과 안내를 함께 개선 | 학습 목표와 화면 행동이 일치함 | 여러 컴포넌트와 테스트를 함께 수정해야 함 | 10/10 |
| B. 문장만 단순화 | 어려운 표현만 쉬운 말로 교체 | 변경 범위가 작음 | 맞는 근거를 하나만 고르는 핵심 문제는 남음 | 6/10 |
| C. 시각 표현만 개선 | 색상과 간격, 결과 화면 안내만 수정 | 빠르게 화면을 정리할 수 있음 | 이해와 학습 행동 문제는 해결하지 못함 | 4/10 |

**결정:** 접근 A를 구현한다. 문구와 상호작용을 별개로 보지 않고, 학생이 읽고 선택하고 확인하는 전체 흐름을 하나의 학습 설계로 다룬다.

## 5. 학생용 문장 설계

### 용어 원칙

- 과학적 경계를 지우지 않으면서 학생의 행동과 가까운 말을 먼저 보여 준다.
- 교사용 설명에는 기존 용어를 남길 수 있지만 학생 화면의 제목과 버튼에는 쉬운 말을 사용한다.
- 한 문장에는 한 가지 뜻만 담는다.
- 버튼은 누른 뒤 일어나는 일을 말한다.
- `완전한 무음`, `실제 안전`, `데시벨 아님` 경계는 삭제하지 않고 짧은 문장으로 나눈다.

### 핵심 표현 변경표

| 현재 표현 | 학생용 표현 | 보조 설명 또는 위치 |
| --- | --- | --- |
| 상대 감지 단계 | 수신기가 받은 소리 단계 | `이 앱 안에서만 서로 비교하는 단계예요.` |
| 상대 감지 결과 | 받은 소리 결과 | 결과 카드 제목 |
| 상대 결과 | 달라진 결과 | 비교 요약 |
| 시험 신호 | 소리의 떨림 | 안내 활동 |
| 한 요소를 통제 | 한 가지만 바꾸기 | 학생 화면 전반 |
| 수치 측정이 아니에요 | 실제 소리 크기를 잰 값이 아니에요 | 안전 약속 |
| 결과를 설명하는 근거를 골라요 | 맞는 근거를 모두 찾아요 | 근거 단계 제목 |
| 정답 수 대신 한 요소를 통제하고 경로를 확인한 과정을 모았어요. | 정답 개수보다, 한 가지만 바꾸고 소리 길을 살핀 과정을 모았어요. | 최종 기록 소개 |

### 안전 약속 문장

안전 화면의 세 약속은 다음 구조로 정리한다.

1. **화면의 물결은 소리를 이해하기 위한 그림이에요.** 실제 소리의 모습은 아니에요.
2. **받은 소리 단계는 실제로 잰 값이 아니에요.** 이 가상 장치 안에서만 비교해요.
3. **가장 낮은 단계도 소리가 없거나 안전하다는 뜻은 아니에요.** 실제 실험은 선생님과 작은 소리로 해요.

미션별 확인 문장은 다음처럼 두 줄로 나눈다.

- 체크 문장: `이 결과는 이 가상 시험 안에서만 비교해요.`
- 보조 문장: `소리가 완전히 없어졌거나 실제로 안전하다는 뜻은 아니에요.`

## 6. 근거 확인 상호작용 설계

### 변경 전

- 참인 근거 2개를 라디오 버튼으로 표시한다.
- 하나만 선택할 수 있다.
- 선택하지 않은 참인 근거가 최종 기록에 반영되지 않는다.

### 변경 후

- 제목을 `맞는 근거를 모두 찾아요`로 바꾼다.
- 두 근거를 각각 체크박스로 표시한다.
- 두 근거를 모두 선택하고 가상 시험 범위 확인을 체크해야 다음 버튼이 활성화된다.
- 첫 번째 근거는 `무엇을 한 가지만 바꾸었는지`를 확인한다.
- 두 번째 근거는 `남은 길 또는 결과를 함부로 넓혀 말하지 않는 이유`를 확인한다.
- 선택한 두 근거를 완료 기록에 함께 보존한다.

### 상태와 데이터 흐름

1. 미션의 근거 목록은 검수된 콘텐츠 두 개를 제공한다.
2. 화면 상태는 하나의 선택 ID가 아니라 선택된 근거 ID 목록을 가진다.
3. 사용자가 근거 카드를 누르면 해당 ID만 선택·해제된다.
4. 선택된 ID가 두 개이고 시험 범위 확인이 완료되면 다음 단계가 열린다.
5. 조건이나 예측을 다시 바꾸면 기존 결과와 근거 선택은 초기화된다.
6. 최종 기록은 두 근거를 모두 읽기 쉬운 문장으로 표시하거나, 화면이 지나치게 길어지지 않도록 `확인한 근거 2개` 요약과 세부 내용을 함께 제공한다.

### 오류와 안내

- 근거를 하나만 고른 상태에서는 오류로 혼내지 않고 `맞는 근거가 하나 더 있어요.`라고 안내한다.
- 시험 범위 확인이 남아 있으면 `가상 시험에서만 비교한다는 약속도 확인해요.`라고 안내한다.
- 아무것도 고르지 않은 초기 상태에서는 버튼만 비활성화하고 오류 문구를 먼저 보여 주지 않는다.

## 7. 조건 비교 화면의 색상과 피드백

### 색상 역할

- 청록: 현재 선택과 정상 진행
- 주황: 기준에서 달라진 조건
- 빨강: 두 가지 이상을 바꾸어 진행할 수 없는 상태
- 초록: 완료 또는 확인 성공에만 사용

### 잘못 두 요소를 바꾼 경우

- 선택 카드 테두리와 안내 문구를 빨강 계열로 표시한다.
- 표의 바뀐 행은 주황색으로 표시해 `변화가 있음`만 전달한다.
- 표 위에 `지금은 2가지가 바뀌었어요.`를 짧게 보여 준다.
- 기존 핵심 안내 `무엇 때문에 달라졌는지 알기 어려워요. 하나만 바꿔요.`는 유지한다.

### 한 요소만 바꾼 경우

- 선택 카드와 진행 안내는 청록 계열을 사용한다.
- `소리원 세기만 바뀌었어요.`처럼 바뀐 한 가지를 직접 말한다.
- 다음 버튼이 활성화되는 변화는 색상뿐 아니라 문장과 버튼 상태로 함께 전달한다.

## 8. 결과 화면과 모바일 안내

### 결과 읽기 순서

결과 영역 첫머리에 다음 안내를 제공한다.

`① 기준 보기 → ② 비교 보기 → ③ 달라진 점 확인`

- 각 장치 카드에는 `1. 기준 조건`, `2. 비교 조건` 번호 표식을 넣는다.
- 모바일의 첫 카드 아래에는 `아래로 내려 비교 조건도 살펴봐요.` 안내를 표시한다.
- 변화 요약에는 `3. 무엇이 달라졌나요?` 제목을 넣는다.
- 다음 버튼은 기존처럼 변화 요약 뒤에 두어 학생이 두 장치를 보지 않고 건너뛰도록 만들지 않는다.
- 모바일에서 결과 영역을 과도하게 가리는 고정 버튼은 사용하지 않는다.

### 결과 카드 표현

- `상대 감지 결과`는 `받은 소리 결과`로 바꾼다.
- 단계 문구 `작게 전달됨`, `가운데 정도로 전달됨`, `크게 전달됨`은 유지한다.
- 각 결과 카드의 보조 설명은 `이 앱 안에서만 비교하는 단계예요.`로 줄인다.

## 9. 컴포넌트와 책임 경계

- 앱 상태 컴포넌트: 선택된 근거 ID 목록, 시험 범위 확인, 다음 단계 잠금 조건을 관리한다.
- 미션 작업 영역: 현재 단계에 맞는 학생용 제목과 행동 안내를 표시한다.
- 조건 비교 영역: 바뀐 조건 수에 따라 중립 변화·오류·정상 상태를 구분한다.
- 결과 경로 영역: 번호가 있는 기준·비교 카드와 모바일 내려보기 안내를 제공한다.
- 최종 기록 영역: 쉬운 완료 문장과 두 근거 확인 결과를 요약한다.
- 콘텐츠 파일: 학생용 문장, 근거 두 개, 업데이트 내역을 검수 가능한 정적 데이터로 유지한다.
- 도메인 규칙: 허용 조합과 한 변인 비교 규칙은 변경하지 않는다.

한 파일이 500줄에 가까워지면 학생용 문장 또는 근거 선택 UI를 별도 파일로 분리한다.

## 10. 테스트 우선 구현 계획

구현 코드를 바꾸기 전에 아래 회귀 테스트를 먼저 작성하고, 기존 동작에서 실패하는 것을 확인한다.

1. 근거 두 개가 체크박스로 렌더링된다.
2. 근거 하나만 선택하면 다음 버튼이 비활성화된다.
3. 근거 두 개와 시험 범위 확인을 모두 선택해야 다음 버튼이 활성화된다.
4. 조건이나 예측을 다시 바꾸면 근거 두 개의 선택이 모두 초기화된다.
5. 학생 화면에 `수신기가 받은 소리 단계`가 표시된다.
6. 학생용 안전 확인 문장이 짧은 체크 문장과 보조 문장으로 분리된다.
7. 두 요소가 바뀐 상태에는 오류 클래스가 적용되고, 한 요소만 바뀐 상태에는 적용되지 않는다.
8. 결과 화면에 세 단계 읽기 안내와 기준·비교 번호가 표시된다.
9. 최신 업데이트 내역에 2026-07-18 학생 사용성 개선 항목이 표시된다.

각 테스트는 먼저 기대한 이유로 실패하는지 확인한 뒤 최소 구현으로 통과시킨다.

## 11. 브라우저 검증 계획

### 데스크톱

- 첫 화면에서 최종 비교 기록까지 6번의 비교를 다시 완주한다.
- 두 요소를 바꾸면 빨간 오류 상태와 주황 변경 행이 함께 보이는지 확인한다.
- 근거 하나만 고른 상태와 두 개를 모두 고른 상태의 버튼 잠금 변화를 확인한다.
- 결과 카드가 `1 기준 → 2 비교 → 3 달라진 점` 순서로 읽히는지 확인한다.

### 모바일 375×812

- 헤더가 기존 121px 수준을 유지하고 도움말 버튼이 한 줄로 보이는지 확인한다.
- 첫 결과 카드 아래의 내려보기 안내가 보이는지 확인한다.
- 문서 전체 가로 넘침이 없는지 확인한다.
- 체크박스 카드와 주요 버튼이 44px 이상인지 확인한다.
- 최종 기록의 쉬운 문장과 비교 표를 확대 없이 읽을 수 있는지 확인한다.

### 공통

- 제목·URL·주요 화면이 정상인지 확인한다.
- 빈 화면이나 프레임워크 오류 화면이 없는지 확인한다.
- 콘솔 오류·경고가 없는지 확인한다.
- 키보드만으로 근거 두 개와 시험 범위 확인을 완료할 수 있는지 확인한다.

## 12. 수용 기준

- 학생 화면에서 `상대 감지 단계` 대신 `수신기가 받은 소리 단계`를 중심 표현으로 사용한다.
- 모든 비교에서 참인 근거 두 개를 모두 선택해야 진행할 수 있다.
- 근거 하나만 선택하면 남은 행동을 쉬운 말로 알려 준다.
- 안전 경계는 삭제하지 않고 한 문장 한 뜻으로 나눈다.
- 잘못된 두 요소 선택이 성공색으로 보이지 않는다.
- 모바일 결과 화면이 아래 내용을 더 보아야 한다는 사실을 알려 준다.
- 최종 기록 소개가 초등 3~4학년이 읽을 수 있는 말로 바뀐다.
- 실제 오디오, 마이크, 데시벨, 실제 재료 성능, 청력 안전 판정을 추가하지 않는다.
- 허용 조합 조회표와 한 변인 비교 규칙을 유지한다.
- 새 업데이트 내역에 개발 날짜와 학생 체감 개선 내용을 기록한다.
- 자동 테스트, 빌드, 린트, 타입 검사, 콘텐츠 검증과 브라우저 실사용 검증을 통과한다.

## 13. 구현하지 않는 범위

- 초등 1~2학년용 전체 읽기 수준 재설계
- 실제 소리 재생과 마이크 측정
- 실제 재료 이름·성능·순위·제품 추천
- 데시벨 또는 음향 계산식
- 점수, 정답률, 배지, 시간 제한
- 학생 계정, 기록 저장, 학급 대시보드
- 외부 과학 검토자의 신규 결과 데이터 검수

## 14. 구현·배포 순서

1. 이 문서를 검토·승인한다.
2. 근거 선택과 학생용 문장 회귀 테스트를 먼저 추가하고 실패를 확인한다.
3. 상태, 콘텐츠, 근거 UI와 결과 화면을 최소 범위로 수정한다.
4. CSS 색상 역할과 모바일 안내를 적용한다.
5. 업데이트 내역과 검증 기록을 갱신한다.
6. 전체 자동 검증과 데스크톱·모바일 실사용 테스트를 수행한다.
7. 변경을 커밋하고 기존 비공개 Sites의 새 버전으로 배포한다.
8. 실제 배포 주소의 HTTP 응답, 제목, 최신 문구와 접근 정책을 확인한다.

## 15. 자체 검토 결과

- 미완성 표시나 결정되지 않은 요구가 없다.
- 학생용 쉬운 표현과 과학적 한계 문구가 서로 충돌하지 않는다.
- 근거 두 개를 모두 확인하는 기능과 최종 학습 목표가 일치한다.
- 기존 허용 조합, 실제 재료 비일반화, 무오디오·무마이크 경계를 유지한다.
- 구현 범위는 기존 React 컴포넌트·콘텐츠·테스트·CSS 안에서 완료할 수 있으며 별도 시스템이 필요하지 않다.
