# 쉬운 말 개선 구현 기록

## 바꾼 내용

- `소리원·수신기·시료·배치·근거·예측·가상 시험`을 `소리 내는 곳·소리 받는 곳·재료 모형·놓는 방법·단서·먼저 생각하기·컴퓨터 모형 실험`으로 바꾸었습니다.
- 미션 질문과 선택지의 문장을 짧게 줄이고, `그대로 둔 것 → 바꾼 것 → 왜 그런지` 순서가 보이도록 결과 화면을 정리했습니다.
- 도착한 소리 크기, 남은 길, 열린 틈을 학생이 읽기 쉬운 말로 바꾸되 실제 측정값이나 실제 안전을 뜻하지 않는다는 범위는 유지했습니다.
- 도움말, 교사용 안내, 최종 기록표, 교육 문서와 업데이트 내역도 같은 용어로 맞췄습니다.

## 확인 결과

- `npm test` 통과: 단위 21개, 빌드, 서버 렌더링 2개
- `npm run typecheck`, `npm run lint`, `npm run validate:content`, `git diff --check` 통과
- 브라우저에서 전체 흐름과 단서 화면 확인
- 375px 모바일에서 문서 폭 360px, 가로 넘침 없음
- 기준 파동 `low/10px`, 비교 파동 `high/20px` 유지
- 브라우저 콘솔 오류·경고 없음
