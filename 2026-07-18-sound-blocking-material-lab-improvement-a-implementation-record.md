# 소리 차단 재료 연구소 개선안 A 구현 기록

- 구현일: 2026-07-18
- 대상: 초등학교 3~4학년 학생
- 기준 문서: `2026-07-18-sound-blocking-material-lab-elementary-usability-plan.md`
- 구현 계획: `docs/superpowers/plans/2026-07-18-elementary-usability-implementation.md`

## 구현한 개선

1. 근거 선택을 라디오 버튼에서 체크박스로 바꾸고, 맞는 근거 2개를 모두 골라야 진행하도록 수정했다.
2. 근거 1개만 고른 경우 `맞는 근거가 하나 더 있어요.`라는 다음 행동 안내를 보여 준다.
3. `상대 감지 단계`, `시험 신호`, `수치 측정`, `한 요소 통제`와 같은 표현을 `받은 소리 단계`, `소리의 떨림`, `실제로 잰 값`, `한 가지만 바꾸기`로 쉽게 바꿨다.
4. 두 가지를 함께 바꾼 선택은 빨간 오류 카드로, 비교표의 바뀐 행은 주황색으로 표시해 성공 상태와 구분했다.
5. 결과 화면에 `① 기준 보기 → ② 비교 보기 → ③ 달라진 점 확인` 순서를 추가했다.
6. 화면이 좁아 두 결과 카드가 세로로 쌓이면 첫 카드 뒤에 `아래로 내려 비교 조건도 살펴봐요.` 안내를 보여 준다.
7. 최종 기록에 비교마다 확인한 근거 2개를 모두 보존하고 표시한다.
8. 모바일 최종 기록은 가로 표 대신 미션별 카드로 보여 결과와 근거가 화면 밖에 숨지 않도록 했다.
9. 2026-07-18 개선 내용을 앱의 `업데이트 내역`에 추가했다.

## 학생 관점 브라우저 확인

- 데스크톱 1440×900에서 시작, 안전 확인, 떨림 안내, 잘못된 두 가지 선택, 올바른 한 가지 선택, 예측, 결과, 근거 흐름을 직접 진행했다.
- 모바일 375×812에서 결과 읽기 순서와 다음 비교 안내가 보이는 것을 확인했다.
- 모바일 문서 너비는 `scrollWidth 360px = clientWidth 360px`로 가로 문서 넘침이 없었다.
- 활성 버튼 중 너비 또는 높이가 44px보다 작은 버튼은 없었다.
- 6번의 비교를 끝까지 완료했고 최종 기록 6행과 근거 12개가 모두 표시되었다.
- 브라우저 콘솔 오류 0개, 경고 0개를 확인했다.

## 자동 검증

- `npm run typecheck`: 통과
- `npm run lint`: 통과
- `npm test`: 단위 테스트 21개, 빌드, 서버 렌더 테스트 2개 통과
- `npm run validate:content`: 콘텐츠 검증 4개 통과
- 모든 코드 파일: 500줄 미만

## QA 화면

- `docs/qa/2026-07-18-improvement-a-invalid-desktop.png`: 두 가지를 함께 바꾼 오류 상태
- `docs/qa/2026-07-18-improvement-a-final-mobile.png`: 모바일 최종 기록 화면
