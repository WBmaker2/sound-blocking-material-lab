# 홈 화면 이동과 소리 크기 표현 개선 계획

## 목표

1. 모든 활동 화면의 헤더에서 첫 화면으로 돌아갈 수 있게 한다.
2. 음높이(주파수)와 소리 크기(진폭·음압과 관련)를 구분해, 미션 예측과 결과에 `높아짐·낮아짐`을 쓰지 않는다.

## 과학·표현 기준

- `높아짐`은 음높이 또는 주파수가 달라지는 뜻으로 읽힐 수 있으므로 이번 비교 결과의 라벨로 사용하지 않는다.
- 이 앱은 실제 소리나 데시벨을 측정하지 않으므로 `소리가 커짐`보다 `도착한 소리 크기 표시가 더 크게 보임`을 사용한다.
- 예측과 결과 라벨은 `더 작게 표시됨·비슷하게 표시됨·더 크게 표시됨`으로 통일한다.
- 파동 막대 높이는 크기 표시를 나타내고, 막대 애니메이션 속도는 모든 결과에서 같게 유지해 주파수 변화로 오해하지 않게 한다.

과학 검토는 주파수와 음높이, 진폭·음압과 소리 크기의 관계를 설명하는 [OpenStax 물리학 자료](https://openstax.org/books/physics/pages/13-2-wave-properties-speed-amplitude-frequency-and-period)와 [NIOSH 음향 자료](https://www.cdc.gov/niosh/docs/98-126/pdfs/98-126.pdf)를 기준으로 한다.

## 구현 범위

- `AppHeader`: `홈 화면` 이동 버튼과 접근성 이름 추가
- `SoundLabApp`: 홈 이동 시 첫 화면 상태로 돌아가는 핸들러 연결
- `content`, `MissionWorkspace`, `FinalRecord`, 도움말·문서·테스트: 예측·결과 표현 통일
- 파동 애니메이션 CSS: 크기 단계에 따른 높이만 데이터로 남기고 속도는 고정
- 업데이트 내역에 개선 기록 추가

## 확인 방법

- 타입 검사·린트·콘텐츠 검사·전체 테스트
- 모바일 브라우저에서 홈 버튼의 위치·터치 영역·가로 넘침 확인
- 미션 1 예측 화면에서 `높아짐·낮아짐`이 사라지고 소리 크기 표현만 보이는지 확인
