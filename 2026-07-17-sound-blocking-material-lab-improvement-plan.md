# 소리 차단 재료 연구소 기능·UI 개선 계획

- 작성일: 2026-07-17
- 대상: `소리 차단 재료 연구소` 첫 교육용 배포본
- 검토 흐름: 첫 화면 → 안전 확인 → 안내 활동 → 미션 비교 → 결과·근거 기록
- 검토 화면: 데스크톱 1280×720, 모바일 375×812

## 검토 결과

### 1. 화면 전환 뒤 이전 스크롤 위치가 남음 — 높음

첫 화면에서 아래로 내려간 상태로 안전 확인이나 안내 활동으로 이동하면 새 화면의 제목이 상단 헤더에 일부 가려진다. 학생이 새 단계가 시작되었다는 사실을 놓칠 수 있다.

**개선:** 화면, 미션, 비교 단계가 바뀔 때 문서 맨 위로 이동한다. 회귀 테스트로 화면 전환 시 스크롤 초기화를 확인한다.

### 2. 모바일 헤더의 도움말 버튼이 세로로 끊김 — 높음

375px 화면에서 `활동 방법`, `교사용`, `업데이트 내역`이 좁은 칸에 배치되어 한 글자씩 세로로 줄바꿈된다. 헤더 높이도 185px까지 커져 실제 학습 영역이 줄어든다.

**개선:** 모바일 헤더를 브랜드·진행 상태·도움말의 세 줄 구조로 정리하고, 도움말 버튼은 가로 한 줄을 유지한다. 브랜드 보조 문구와 버튼 글자 크기도 초등학생이 읽을 수 있는 수준으로 올린다.

### 3. 진행 중 로고를 누르면 즉시 초기화됨 — 높음

미션을 진행하다 로고를 누르면 안내 없이 첫 화면으로 돌아가며 현재 기록이 사라진다.

**개선:** 진행 중일 때만 `처음부터 다시 할까요?` 확인 대화상자를 표시한다. `계속 연구하기`를 기본 초점으로 두고, 사용자가 명시적으로 선택한 경우에만 초기화한다.

### 4. 미션 안에서 진행 막대가 움직이지 않음 — 중간

조건 비교, 결과 예측, 결과 확인, 근거 비교의 네 단계가 바뀌어도 진행 막대 값이 같다. 버튼을 눌러도 전체 진행에 반영되지 않아 반응성이 약하다.

**개선:** 준비 단계와 여섯 번의 비교 활동을 기준으로 단계별 진행률을 계산한다. 진행 막대에 `role="progressbar"`와 현재 값을 제공한다.

### 5. 일부 보조 문구가 모바일에서 너무 작음 — 중간

브랜드 설명, 미션 설명, 표 글자 일부가 10~13px대로 표시된다. 핵심 활동은 가능하지만 초등학생에게는 읽기 부담이 있다.

**개선:** 모바일의 보조 문구 최소 크기를 높이고 표는 14px 이상으로 조정한다. 정보 밀도가 높은 곳은 글자를 줄이기보다 간격과 배치를 정리한다.

## 구현 범위

1. 화면 및 단계 전환 스크롤 초기화
2. 안전한 재시작 확인 대화상자
3. 비교 단계까지 반영하는 진행률 계산과 접근성 속성
4. 375px 모바일 헤더 재배치와 보조 문구 크기 개선
5. 앱 안 `업데이트 내역`에 2026-07-17 개선 기록 추가
6. 관련 컴포넌트 회귀 테스트와 브라우저 재검증

## 완료 기준

- 안전 확인, 안내 활동, 미션 및 다음 비교로 이동할 때 제목이 헤더 아래에서 온전히 보인다.
- 진행 중 로고를 눌러도 확인 전에는 화면과 기록이 유지된다.
- 확인 대화상자의 취소·초기화 선택이 키보드와 화면 읽기 환경에서 구분된다.
- 미션의 네 단계가 바뀔 때 진행률 값이 증가한다.
- 375×812에서 헤더 버튼 글자가 세로로 끊기지 않고 가로 넘침이 없다.
- 자동 테스트, 빌드, 린트, 타입 검사와 실제 브라우저 흐름을 통과한다.

## 검토 경계

- 실제 데시벨, 실제 재료 성능, 마이크 입력 또는 청력 안전 판정을 추가하지 않는다.
- 가상 시료와 검토된 허용 조합 표를 유지한다.
- 외부 과학 검토자 2인의 독립 검수는 별도 과제로 남긴다.

## 구현 결과

- 2026-07-17: 계획한 다섯 개선 항목을 모두 구현했다.
- 화면과 비교 단계가 바뀔 때 스크롤 위치를 0으로 초기화한다.
- 진행 중 로고를 누르면 기록 삭제 확인 대화상자를 표시한다.
- 미션 안의 조건·예측·결과·근거 단계마다 진행률이 증가한다.
- 375px 모바일 헤더 높이를 185px에서 121px로 줄이고 도움말 버튼의 세로 글자 끊김을 제거했다.
- 모바일 표 글자를 14px로 높였으며 가로 넘침이 없음을 확인했다.
- 자동 테스트 18개, 타입 검사, 린트와 인앱 브라우저 검증을 통과했다.

상세한 전후 비교와 점수는 `.gstack/design-reports/design-audit-sound-blocking-material-lab-2026-07-17.md`에 기록했다.
